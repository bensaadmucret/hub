    <form method="post" class="space-y-6" data-controller="onboarding-validation" data-action="submit->onboarding-validation#submitForm">
        <input type="hidden" name="_token" value="{{ csrf_token('onboarding_step3') }}">
        <div>
            <label for="targetScore" class="block text-sm font-medium text-gray-700">
                Objectif de score (0-100)
            </label>
            <div class="mt-1">
                <input 
                    id="targetScore" 
                    name="targetScore" 
                    type="number" 
                    min="0" 
                    max="100" 
                    required 
                    value="{{ targetScore }}"
                    class="appearance-none block w-full px-3 py-2 border {% if errors.targetScore is defined %}border-red-500{% else %}border-gray-300{% endif %} rounded-md shadow-sm placeholder-gray-400 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm"
                    data-onboarding-validation-target="number"
                    data-action="blur->onboarding-validation#validateNumber"
                    data-min="0"
                    data-max="100"
                >
                {% if errors.targetScore is defined %}
                    <p class="mt-1 text-sm text-red-600">{{ errors.targetScore }}</p>
                {% endif %}
            </div>
            <p class="mt-1 text-xs text-gray-500">
                Quel score visez-vous pour votre examen ? Cela nous aidera à adapter votre plan d'étude.
            </p>
        </div>

        <div>
            <label for="studyHoursPerWeek" class="block text-sm font-medium text-gray-700">
                Heures d'étude par semaine (1-80)
            </label>
            <div class="mt-1">
                <input 
                    id="studyHoursPerWeek" 
                    name="studyHoursPerWeek" 
                    type="number" 
                    min="1" 
                    max="80" 
                    required 
                    value="{{ studyHoursPerWeek }}"
                    class="appearance-none block w-full px-3 py-2 border {% if errors.studyHoursPerWeek is defined %}border-red-500{% else %}border-gray-300{% endif %} rounded-md shadow-sm placeholder-gray-400 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm"
                    data-onboarding-validation-target="number"
                    data-action="blur->onboarding-validation#validateNumber"
                    data-min="1"
                    data-max="80"
                >
                {% if errors.studyHoursPerWeek is defined %}
                    <p class="mt-1 text-sm text-red-600">{{ errors.studyHoursPerWeek }}</p>
                {% endif %}
            </div>
            <p class="mt-1 text-xs text-gray-500">
                Combien d'heures par semaine pouvez-vous consacrer à vos études ? Cela nous permettra de créer un planning réaliste.
            </p>
        </div>

        <div class="flex justify-between">
            <a 
                href="{{ path('app_onboarding_step2') }}" 
                class="inline-flex justify-center py-2 px-4 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500"
            >
                Précédent
            </a>
            <button 
                type="submit" 
                class="inline-flex justify-center py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500"
            >
                Continuer
            </button>
        </div>
    </form>
